const prefix = 'SPACESTOCK';

export const HOME_SCREEN = `${prefix}.HOME_SCREEN`;
export const UNIT_LIST_SCREEN = `${prefix}.UNIT_LIST`;
export const SEARCH_SCREEN = `${prefix}.SEARCH_SCREEN`;
export const UNIT_DETAIL = `${prefix}.UNIT_DETAIL`;
